apiVersion: v1
kind: Service
metadata:
  name: website
  namespace: website-bolt
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: website
